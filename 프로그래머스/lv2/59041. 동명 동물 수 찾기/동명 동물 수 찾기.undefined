-- 코드를 입력하세요
# SELECT NAME,COUNT(NAME)
# FROM ANIMAL_INS 
# WHERE COUNT(NAME) > 1 AND NAME IS NOT NULL
# GROUP BY NAME

SELECT NAME,COUNT(NAME)
FROM ANIMAL_INS 
GROUP BY NAME
HAVING COUNT(NAME) > 1
# WHERE NAME IS NOT NULL
ORDER BY NAME
